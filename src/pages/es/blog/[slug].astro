---
export async function getStaticPaths() {
  // TODO: Fase 5 — fetch artículos publicados desde Supabase
  return [];
}
import BaseLayout from '../../../layouts/BaseLayout.astro';
const { slug } = Astro.params;
const lang = 'es';
---
<BaseLayout title={`${slug} | Güinez Galaz Abogados`} lang={lang}>
  <main><article><h1>{slug}</h1></article></main>
</BaseLayout>
